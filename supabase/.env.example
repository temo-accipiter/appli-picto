# ---------- .env -------
# Variables secrètes côté serveur (Supabase CLI)
# ⚠️ NE JAMAIS exposer ces variables côté client

# --- Projet / CLI / DB ---
SUPABASE_SERVICE_ROLE_KEY=eyJhb...
SUPABASE_PROJECT_REF=tklcztqoqvni...
SUPABASE_DB_HOST=aws-0-us-eas...
SUPABASE_DB_PORT=5432
SUPABASE_DB_NAME=postgres
SUPABASE_DB_USER=postgres.tklcztq...
SUPABASE_DB_PASSWORD=iESA...

# --- Stripe (Edge Functions) ---
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Prix par défaut côté Edge Function (non secret)
STRIPE_PRICE_ID=price_...

# Turnstile (clé secrète)
TURNSTILE_SECRET_KEY=0x4AAA...

# 👇 Ajout conseillé (valider les retours Stripe côté Edge Function)
# Plusieurs hôtes séparés par des virgules, sans protocole
ALLOWED_RETURN_HOSTS=localhost:5173

# ---------- .env.local -------
# Variables secrètes locales côté serveur (Supabase CLI)
# ⚠️ NE JAMAIS exposer ces variables côté client
# Ce fichier peut contenir des surcharges locales

# Exemple de surcharge locale :
# SUPABASE_DB_PASSWORD=password_local_different
# STRIPE_SECRET_KEY=sk_test_local_key